# Core Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# LLM & RAG
langchain==0.3.14
langchain-groq==0.2.4
langchain-chroma==0.2.2
langchain-community==0.3.14

# Vector Store
chromadb==0.6.3

# Embeddings - HuggingFace (OPCIONAL - solo si usas EMBEDDING_PROVIDER=huggingface en local)
# Comentado por defecto porque consume +400MB de RAM (no apto para Render Free)
# Descomenta solo si vas a usar embeddings locales con suficiente RAM
# langchain-huggingface==0.1.2
# sentence-transformers==3.4.1

# Google Generative AI (Gemini - Free tier available)
# Nota: google-generativeai y langchain-google-genai tienen dependencias compartidas
# Dejamos que pip resuelva las versiones compatibles automáticamente
google-generativeai>=0.8.0
langchain-google-genai>=2.0.0

# Mistral AI (Free tier available)
mistralai==1.5.1
langchain-mistralai==0.2.7

# Document Loaders
pypdf==5.1.0
python-docx==1.1.2

# Data & Utilities
pydantic==2.10.5
pydantic-settings==2.7.1
python-dotenv==1.0.1

# CORS & HTTP
httpx==0.28.1
